class Config {
  public static var icons(default, null) = {
    add : 'plus-circle',
    addMenu : 'plus-square',
    remove : 'ban',
    dropdown : 'reorder',
    checked : 'toggle-on',
    unchecked : 'toggle-off',
    switchtype : 'bolt',
    code : 'bolt',
    value : 'pencil',
    reference : 'link',

    bool : 'check-circle',
    text : 'pencil',
    number : 'superscript',
    date : 'calendar',
    array : 'list',
    object : 'table',
  };

  public static var selectors(default, null) = {
    app : '.card'
  };
}
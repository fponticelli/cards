class Config {
  public static var icons(default, null) = {
    add : 'plus-circle',
    remove : 'ban',
    dropdown : 'reorder',
    checked : 'toggle-on',
    unchecked : 'toggle-off',
    switchtype : 'bolt',
    code : 'bolt',
    value : 'pencil',
    reference : 'link',
  };

  public static var selectors(default, null) = {
    app : '.card'
  };
}
class Config {
	public static var icons(default, null) = {
		add : 'plus-circle',
		remove : 'ban',
		dropdown : 'reorder',
		checked : 'dot-circle-o',
		unchecked : 'circle-o',
		switchtype : 'bolt',
	};

	public static var selectors(default, null) = {
		app : '.card'
	};
}
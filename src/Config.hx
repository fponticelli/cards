class Config {
	public static var icons(default, null) = {
		add : 'plus',
		remove : 'minus',
		dropdown : 'arrow-down',
		checked : 'check-square-o',
		unchecked : 'square-o',
		switchtype : 'arrows',
	};

	public static var selectors(default, null) = {
		app : '.card'
	};
}